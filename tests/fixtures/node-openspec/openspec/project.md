# Projeto

Serviço de totalização de pedidos. Legado do OpenSpec: este arquivo é a
forma antiga do `config.yaml` — a FASE 1 deve reconhecê-lo e avisar que
`openspec update` faz a migração, em vez de gravar um config por cima.
