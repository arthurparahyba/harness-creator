{
  "name": "pedidos-api",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "vitest": "^2.0.0", "eslint": "^9.0.0", "typescript": "^5.5.0", "prettier": "^3.3.0"
  }
}
