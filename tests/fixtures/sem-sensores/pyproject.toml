[project]
name = "relatorios"
version = "0.3.1"
description = "Gerador de relatorios de vendas a partir de planilhas"
requires-python = ">=3.10"
dependencies = [
    "openpyxl>=3.1",
    "requests>=2.31",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
